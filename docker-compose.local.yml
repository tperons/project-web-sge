volumes:
  project_web_sge_local_postgres_data: {}
  project_web_sge_local_postgres_data_backups: {}
  

services:
  django:
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: project_web_sge_local_django
    container_name: project_web_sge_local_django
    depends_on:
      - postgres
    volumes:
      - /app/.venv
      - .:/app:z
    env_file:
      - ./.envs/.local/.env
    ports:
      - '8000:8000'
    command: /start.sh

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: project_web_sge_production_postgres
    container_name: project_web_sge_local_postgres
    volumes:
      - project_web_sge_local_postgres_data:/var/lib/postgresql/data
      - project_web_sge_local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.env
